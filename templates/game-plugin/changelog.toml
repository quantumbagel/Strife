# Shown in /strife about → Changes.
# Keep the latest version in sync with plugin.toml. Newest first.

[[release]]
version = "1.0.0"
date = ""  # TODO: YYYY-MM-DD of the first ship
summary = "Initial release."
added = [
  "Pass to win, easy bot",
]
