# Bot history. Shown in /strife about → Changes.
# Newest first. Game API versions go in platform.toml.

[[release]]
version = "1.0.2"
date = "2026-09-10"
summary = "Slash autocompletes explain empty lists."
added = [
  "Slash autocompletes explain empty lists (not hosting a lobby, no bots, no replays)",
]

[[release]]
version = "1.0.1"
date = "2026-09-10"
summary = "Chess variants, a lobby fix, shorter docs."
added = [
  "Chess variants, clocks you type, pick white or black",
]
changed = [
  "Shorter docs",
]
fixed = [
  "Remove-bot menu showed raw emoji codes",
]

[[release]]
version = "1.0.0"
date = "2026-09-07"
summary = "Install games from git. Changes tab in About."
added = [
  "strife/install, update, and uninstall",
  "Each game can ship its own emoji",
  "Changes in /strife about",
  "GitHub template for new games",
  "Privacy page",
]
changed = [
  "Game libraries live in plugin.toml, not the bot package",
  "Catalog shows each game's last update date",
]
fixed = [
  "Rematch, forfeit, and lobby start could leave people stuck",
]

[[release]]
version = "0.6.0"
date = "2026-08-21"
summary = "Games are plugins. Discord is just the host."
added = [
  "Game API separate from the live Discord session",
  "Sharding, a Postgres pool, and a CPU worker pool",
]
changed = [
  "Games deal their own roles; the lobby Roles tab is gone",
  "Games don't import the live session anymore",
]
removed = [
  "Old GameSession import path",
]

[[release]]
version = "0.5.0"
date = "2026-08-15"
summary = "Docker, matching UI, launch games on."
added = [
  "Docker image",
  "Lobbies need a normal text channel (no forums)",
]
changed = [
  "Catalog, about, settings, profile, and errors look the same",
  "Launch games are on; the test game stays off",
]
fixed = [
  "Spyfall bots during discussion",
  "A restart could leave people stuck in a match",
]

[[release]]
version = "0.4.0"
date = "2026-08-01"
summary = "Public invite."
added = [
  "Public invite, plus strife/sync, strife/emoji, and strife/dbreset",
]

[[release]]
version = "0.3.0"
date = "2026-07-14"
summary = "Chess, turn clocks, hung games stay isolated."
added = [
  "Chess (/chess move)",
  "Turn timeouts, strikes, skip, and bot takeover",
  "Join, leave, and ready as slash commands too",
]
changed = [
  "A hung game only kills that match",
]
fixed = [
  "Coup replay and timeout text",
  "Tic-Tac-Toe bots and the test game's board images",
]

[[release]]
version = "0.2.0"
date = "2026-07-08"
summary = "Social games, peek, rematch that doesn't double-book you."
added = [
  "Coup, Mafia, Spyfall, and Liar's Dice",
  "Connect Four, and Tic-Tac-Toe with bots",
]
changed = [
  "Join, leave, ready, and errors share one set of copy",
]
fixed = [
  "Rematch could open a second lobby while the first was still live",
  "Outcome mentions, and Mafia bots counting toward the vote",
]

[[release]]
version = "0.1.0"
date = "2026-06-22"
summary = "Lobbies, a public game thread, rematch."
added = [
  "/play, ready-up, and a public {Game} (#code) thread",
  "/strife catalog, profile, replay, forfeit, about, and server channel",
  "Private lobbies with join requests and a blacklist",
  "Custom emoji on catalog cards",
  "Rematch on the old lobby message; the thread locks when the game ends",
]
